#include <stdio.h>
/**
 * main - entry point
 *
 * Description:A program that prints the alphabet in lowercase
 *
 * Return: always 0(success)
 */

int main(void)
{
	int num = 0;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);

}

