Vi 101-password
Type ":set binary"
Type ":set noeol
Type ":set list"
Press "i" for insert mode 
Then type "Hol"
Use "Ctrl+v" and "Ctrl+v" to get the last part of it
Git add ., git commit and git push
